const stream = ipfs.cat(cid);
//   const decoder = new TextDecoder();
// let dat1a = '';

// for await (const chunk of stream) {
//   // chunks of data are returned as a Uint8Array, convert it back to a string
//   dat1a += decoder.decode(chunk, { stream: true });
// }

// console.log(dat1a);
    